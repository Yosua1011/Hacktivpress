module.exports = function (str) {
  var tobeslug = str.toLowerCase().replace(/[^a-z0-9 ]|\r?\n|\r/gmi, "").split(' ')
  var newDate = new Date()
  var getTime = `${newDate.getMinutes()}${newDate.getSeconds()}`
  tobeslug.unshift(getTime)
  return tobeslug.join('-')
}
